I think we have a very complete playwright test suite, but maybe we could create more scenarios  for the implemented features, since for example the CARTO project has approximately 130 tests and TENNIS only 79 where only 58 are ran because of the unimplemented scenarios, I want you to create several more scenarios, document it first and then we could consider implement the tests, do a high research in the codebase.
