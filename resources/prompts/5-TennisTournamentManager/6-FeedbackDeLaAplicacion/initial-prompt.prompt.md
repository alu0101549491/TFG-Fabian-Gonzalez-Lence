The client gave us some feedback on the app TENNIS, check all the anomalies and the checkmarks that applied and make a planning doc file and a Copilot TODO to implement the features from smaller to bigger ones step by step. Also check that everything said that keeps in scope of the known scope of the app, we don't want things to be implemented out of the previously planned scope. Don't modify the FEEDBACK file, have in consideration every listed item at that and if a feature is marked as anything but Covered and is a feature actually existent both at frontend and backend, create a new entry in a new document file that will work as a guide to replicate the supposely "not covered features".
