The last thing the client asked me to do was to modify the interaction of the files with DropBox. Currently, every time a project is created, a folder is created in CartographicProjects/ with the project code, but, on the one hand, the files that are uploaded from the page appear on the page and in DropBox, but the files uploaded from DropBox do not appear on the page. Another thing is that when a project is deleted, the respective directory in Dropbox remains active, so if another project with the same code is created, the directory is overwritten.
